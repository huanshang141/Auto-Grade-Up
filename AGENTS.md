# 项目记忆

## 表达规范

- **禁止使用 ASCII 字符绘制架构图/流程图/示意图**（如 `┌─▼→` 框线图）。所有图表一律使用 Mermaid 代码块（`graph`、`sequenceDiagram`、`stateDiagram-v2` 等），Markdown 渲染器可原生显示。文档（设计文档、README、说明文字）与对话输出均适用。

## 语言规范

1. 禁止生造词汇。
2. 禁止中文的字词缩写：原本是两个字的词语不允许缩为一个字。正确用法「崩溃、强化、升级」，错误用法「崩、强、升」。
3. 禁止不必要的对比句式：不使用「不是……而是……」去解释简单易懂的事实。
4. 禁止互联网黑话，使用任何人都能理解的日常用语。
5. 专业术语必须在开头予以说明，说明本身同样遵循上述规范。
6. 英文区分两类：通用技术用语（如 `JSON`、`OCR`、`trace`）允许保留；项目自造的英文命名（如 `candidates`、`initial`）禁止单独出现在正文里，必须以中文为主体，英文用括号辅助说明，例：筛选条件（candidates）。

以上规范适用于技术方案讨论、评审提问、文档撰写与代码注释。

## 文档修订规范

- 禁止以增加勘误注记的方式修订原有文档（在原文旁补「勘误」「注」一类说明）——这会让文档更难读。需要修正时直接修改原文，文档始终只保留一种现行口径。
