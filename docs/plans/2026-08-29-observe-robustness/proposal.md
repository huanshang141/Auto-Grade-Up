# M2 补强提案：强化页新形态解析与九张新夹具入测

## 背景

三轮拷问（本文件夹 `discussion.md`）确认：强化页存在成长结算形态
（旧值 → 新值 ↑，取新值入模）、「新」角标（新解锁词条）、满级「MAX」
标记三种 M2 未覆盖的形态，现行解析会失败。第二、三批补拍共 9 张夹具
（`reference/pic3/` 7 张 + `reference/pic4/` 2 张）已验收、待入测。

## 目标

截图 → 框架识别 → 解析核心 → 断言的离线全链路对 27 张夹具全绿；
识别结果转储保留带圈数字（为 M2.5 的强化次数读取保留信息）。

## 范围

解析核心强化页行清洗扩展；流水线 MAX 处理与带圈数字保留；截图来源
归整（四个批次目录合并为 reference/pic/ 单一来源，需求方定案）；九张
真值表补行；全量回归。

## 非目标

强化次数入模、强化剪枝与次数规则体系（M2.5）；真机相关（M3）。

## 上游约束

`docs/specs/observation.md`「强化页的读取时机与行形态」节（拷问轮已
更新，正本先行，本变更落地后无折叠差异）。
