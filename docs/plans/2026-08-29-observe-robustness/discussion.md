# 观测机构强化页稳健性讨论记录（2026-08-29）

> 性质：M2 验收后的补强讨论底稿，供后续契约修订、解析实现与 M3 设计引用。
> 已确认结论的正本在 `docs/specs/observation.md`（强化页的读取时机与行形态节），
> 决策沉淀到 `docs/adr/` 与 `CONTEXT.md` 时以那些文档为准，本文不保持现行口径。

## 起因

第二批补拍（`reference/pic3`）验收发现：`E5_enhance_lv8_preview.png` 是强化
执行机构**刚完成一次强化**的界面——第 4 条副词条显示「防御力 5.8% → 11.1% ↑」。
此前 18 张夹具全部是静止形态，问题因此暴露：单件强化流程中，强化页读取器的
主要使用时机恰是「每个强化回合结束后重新读取、判定是否继续」，若解析核心
不支持结算形态，每个经过词条成长的回合都无法判定（整行读取失败）。

## 讨论过程中的形态盘点（含被需求方否定的一档）

初始分析按四个时机盘点强化页形态：

| 时机 | 初始假设的形态 | 后续结论 |
| --- | --- | --- |
| 进入强化页、尚未放入素材 | 静止单值 | 成立 |
| 放入素材后、未点强化 | 假设有「旧值 → 新值 ↑」预览 | **被否定**：界面无任何变化 |
| 强化结算后 | 「旧值 → 新值 ↑」 | 成立，且为回合后读取的常态输入 |
| 满级 +20 进入强化页 | 素材区形态未采样 | 形态未知，判定所需字段不受影响（低风险） |

## 需求方纠正与补充（2026-08-29，原文要点）

1. **不存在「放入素材后预览」阶段**：放入素材后、未点强化时界面不会有任何
   变化；只有点击强化、等待一两秒强化完成之后，才会出现「旧值 → 新值 ↑」。
2. **结算标记长期保留**：「旧值 → 新值 ↑」在强化页面中一直保留，直到新的
   强化完成（更新）或者退出强化界面。
3. **强化次数的重要性**：除了数值，词条的强化次数（roll 次数）未来也可能
   进入决策——计划在决策机构中推出强化剪枝（提前放弃不可能达到规则要求的
   圣遗物），以及另一套规则体系（规则表现为设置词条的强化次数）。

## 分析结论（已落契约正本）

- 「等待标记消退」方案被排除（标记不自动消退），契约修订是唯一路径。
- 同一「双数值行」视觉不存在语义二义性的来源（无预览态），解析取**新值**
  （当前值）即可，无需感知时机；但解析仍不依赖箭头符号（按数值个数判断），
  防箭头误读。
- 读取时机纪律归执行机构（M3）：只在「进入未放入」与「结算后」两个时机
  调用强化页读取器；时序异常由等级核对兜底（安全停止），不属于解析核心。
- 带圈数字标记与结算标记互相独立、长期保留，清洗约定不变。

## 未来方向：强化次数的观测来源（开放，待细化）

需求指向：决策机构未来需要「每个词条的强化次数」。候选来源各有待验证问题：

1. **界面标记识别**：词条旁的带圈数字若语义可考（如获得节点序号或 roll
   次数），OCR 可读——但现行清洗规则把它删除，需要改为保留并解析；E4
   （四条全 ①）与 fig2（②攻击力、①生命值）的语义尚未定论。
2. **数值反推**：词条当前值 = 初始值 + 若干次随机区间成长，无法精确反推
   roll 次数，只可能给出候选范围。
3. **执行机构记账**：回合间 diff 词条数值变化本地累计——只对「全程跟随
   强化」的圣遗物有效；列表页扫描遇到的圣遗物无历史可记，两个读取器的
   输出一致性（ADR-0005 同一数据模型）如何维持需要设计。

以上开放问题在拷问（grilling）环节逐项细化，结论落 ADR 与 CONTEXT.md。

## 第一轮拷问结果（2026-08-29）

**查证结论（带圈数字语义定论）**：带圈数字 ①②③ 是词条被强化的次数显示，
原神 5.3 版本（「炽烈的还魂诗」）官方新增功能，更新说明原文「圣遗物的
追加属性增加了被强化次数的数字显示」（来源：米哈游官方更新说明
ys.mihoyo.com/main/news/detail/127804）。与样本完全吻合：E4 为 5 星 +16、
四次成长分到四条、全部 ①。此前的「候选来源 1」从待验证升级为已确认的
数据来源；「候选来源 3」（执行机构记账）不再必要。

**问答记录**：

| 问题 | 需求方回答 | 落点 |
| --- | --- | --- |
| Q1 主词条结算形态 | 不显示前后对比、只有新值；主词条数值不用太关心，但主词条字段很可能作为规则条件（读取准确性仍重要） | 契约：结算形态仅副词条行 |
| Q2 多回合标记刷新 | (a) 整批刷新：每次强化后只有本次成长的词条带「→」，上一回合的标记消失 | 契约：标记整批刷新 |
| Q3 E4 截面状态 | 退出后重新进入的静止界面 | 与保留规则自洽，无新结论 |
| Q4 转储保留带圈数字 | 保留，重录转储 | 本补强任务清单 |
| Q5 满级强化页样本 | 满级圣遗物不主动进强化页（系统目标为强化）；强化界面中强化到 +20 的回合后界面，后续提醒补拍 | 待补拍 E6，M3 前提醒 |
| Q6 双数值清洗范围 | 仅强化页读取器 | 契约已写，实现按此 |
| Q7 强化次数体系归属 | **M3 单件流程内就要考虑**（不是 M3 后） | M3 规模与观测接口预留调整 |
| Q8 执行方式 | 拷问结果落文件，再进行下一轮拷问 | 本文与后续 |

**第二轮待细化的问题**（由 Q7 的 M3 内定案解锁）：强化次数入模的输出
结构、无标记与识别失败的语义区分、新增词条解锁瞬间的结算形态、强化剪枝
的输入（档案是否扩充单次成长上限表）、强化次数的规则文件语法方向。

## 第二轮拷问结果（2026-08-29）

**问答记录**：

| 问题 | 需求方回答 | 落点 |
| --- | --- | --- |
| Q9 强化次数输出结构 | 按推荐：词条值对象扩展（代号 + 数值 + 强化次数，次数可空） | M1 数据模型修订方向，M3 落地 |
| Q10 无标记语义 | 按推荐：无标记即 0 次，OCR 漏读风险记录、M3 真机验证 | 契约已含语义基础 |
| Q11 新解锁词条形态 | **普通单值，不是前后对比** | 契约：新解锁形态 |
| Q12 剪枝输入 | 数值规则按推荐（档案扩充分星级单次成长上限表、决策机构自动推导）；**次数规则直接用当前强化次数 + 剩余强化次数判断能否满足要求，不需要数值表** | 档案扩充 + 决策机构设计输入 |
| Q13 次数体系形态 | 按推荐：独立规则体系、只在强化回合判定时机求值，条件格式沿用现有条件树写法 | M3 规则体系设计输入 |

**第三批截图验收（reference/pic4，1920×1080）**：

| 文件名 | 验收内容 |
| --- | --- |
| `E6_enhance_lv20_max.png` | 满级强化页：+20 旁带红色「MAX」标记（等级区域识别需处理，避免「+20 MAX」解析失败）；素材档位、摩拉、强化按钮整体消失，改显「已达到当前等级上限」；左侧多出「重塑」标签页；副词条 ①暴击率 3.5% → 6.6% ↑（结算形态）+ ③生命值 18.1%（累计 3 次）——同时实证「整批刷新」（只有本次成长的暴击率带箭头）与带圈数字累计语义 |
| `E7_enhance_lv4_new_stat.png` | 新解锁词条形态：5 星空之杯 +4 结算后，第 4 条「新 攻击力 4.7%」——金色「新」角标 + 普通单值（与 Q11 回答一致）；主词条岩元素伤害加成 14.9% 单值（实证 Q1 主词条无前后对比）；三条初始词条无标记 |

**解锁不计强化次数的推证链（2026-08-29，全样本自洽）**：带圈数字 =
纯强化（roll）次数、解锁新词条不计入——E7 解锁词条带「新」角标且无带圈
数字（roll 0）；fig2 的生命值（+4 解锁）在退出重进后显示 ①（roll 1 次，
来自 +8/+12/+16 三次 roll 中的一次）；E4（5 星 +16、初始 4 条）四次 roll
分到四条、全部 ①；E5 的防御力（+4 解锁、初始 5.8%）在 +8 被 roll 一次
显示 ① 与「5.8% → 11.1%」。「新」角标与结算标记同属回合后短暂标记，
退出界面后消失（fig2 实证）。

## 第三轮拷问结果（2026-08-29，收敛轮）

**问答记录**：

| 问题 | 需求方回答 | 落点 |
| --- | --- | --- |
| Q14 「新」角标语义链 | 按推荐确认（解锁不计次数、roll 0、退出后消失） | 契约已落 |
| Q15 两套规则求值关系 | 按推荐：与合并单布尔，任一不通过即止损 | M3 执行机构接口输入 |
| Q16 次数识别失败语义 | 修正补充：OCR 之外维护一套图像对比备选——带圈数字预先录制成模板，走 TemplateMatch（MaaFramework 流水线原生支持）；OCR 与模板互为备份 | 契约：识别方案 |
| Q17 里程碑归属 | **次数规则需较大幅度更改决策机构，与 M3 目标不一致——插入 M2.5 里程碑**，专门承接强化剪枝、次数体系等功能开发；M3 保持单件流程真机联调 | 主 spec §14 已插入 M2.5 行 |

**收敛状态**：设计树各分支均已定论——结算形态解析（取新值）、「新」角标
与强化次数语义、满级形态处理、roll 输出结构（词条值对象扩展、次数可空、
无标记即 0、识别失败走读取失败 + 模板备选）、剪枝输入（数值条件用档案
成长上限表、次数条件用当前次数 + 剩余次数）、两套规则与合并单布尔、
里程碑切分（本补强 → M2.5 决策机构扩展 → M3 真机联调）。无遗留开放
问题；后续文档产出：本补强落地（近期任务：解析实现 + 九张入测）、
M2.5 立项时沉淀 ADR 与规则文件格式修订。



