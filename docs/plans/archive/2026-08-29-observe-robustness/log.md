# M2 补强执行日志

> 本文件夹的变更记录；永不删除。计划与决策见 `proposal.md`、`design.md`、
> `tasks.md`，拷问结论见 `discussion.md`。

## 2026-08-29 · Task 4.1
- Tried:  九张真值一次写全（真值来源 README 验收记录 + 转储核对）；写行前
          先核对九张新转储的置信度下限（E6 最低 0.667，来自标记误读框，
          高于 0.6 断言线）；E6 的满级形态用 exp=None 哨兵表示——断言分支
          改为「extras 仅剩指纹」的整字典比较
- Result: 27 张端到端一次全绿（11.54 秒），全量 335 passed；E5 的结算行
          取新值（防御 11.1）、E6 的 MAX 去除与 0/3 误读剥离、E7 的「新」
          角标剥离、E4 的四条 ① 剥离、E5 面包屑前缀匹配切分全部经真实
          识别验证
- Now:    E4/E7 沿用字段套装按 D6 传占位字符串（强化页不显示套装、未从
          列表页采样）；E6 沿用字段用真实值（同名圣遗物 L7 采样过套装
          影中沉凝的幻灭，5 星由满级充能 51.8% 实证）
- Convention: ENHANCE_TRUTH 的 exp 列传 None 表示满级形态（extras 仅剩
          指纹的整字典断言）；补拍夹具的真值溯源以 README 验收记录为准

## 2026-08-29 · Task 3.1
- Tried:  流水线两处（等级节点加「MAX」纠错、副词条节点撤销 ①~⑤ 删除）+
          重录后即时冒烟暴露四个真实识别形态，随任务就地调优——
          (1) E6/E4 的 ①③ 被误读为「0」「3」（单框或并框），结算合并会
          把词条名吞空 → 解析层新增行首一至两位孤立数字剥离（词条名十族
          不含数字，行首短数字必为标记误读）；(2) E6 满级经验条图形被
          误读为杂字「∴」→ exp 节点加 expected「[0-9]+/[0-9]+」过滤
          （only_rec 下实测生效）；(3) E5 面包屑「/」被 OCR 读丢 → 契约
          修订：分隔符读丢时按对照文档部位名前缀匹配切分（匹配不到仍
          失败），实现 _split_breadcrumb；(4) E5 的「↑」独立成框——既有
          结算合并已覆盖（非数值 token 丢弃），无需改动
- Result: 27 份转储入库；旧 18 份中仅 fig2/fig4 的「攻击力」→「②攻击力」
          （带圈数字保留进转储，D5 预期效果），其余逐字节一致；解析冒烟
          26 ok / 1 fail（fig5 预期遮挡）；单元测试 53 passed、全量
          326 passed
- Now:    异常清单 4 条全部预期内（E6 的 exp/mora/fodder_tier 满级形态
          三项 + fig5 弹窗遮挡一项）；「新」角标与词条名并框（E7「新
          攻击力」）由行首剥离覆盖，独立成框由空行丢弃覆盖
- Convention: 识别形态与解析能力不一致时，先修契约再补实现（面包屑切分
          修订走上游工件）；转储保留的标记误读形态（①→0/③→3）由解析层
          鲁棒处理，不在识别层删除——保持 D5「转储信息完整」约定

## 2026-08-29 · Task 2.1
- Tried:  TDD——先写 5 个强化页用例 + 1 个列表页回归用例跑红（现行实现遇
          结算行在数值解析层 ValueError、4 failed），再实现转绿。算法按
          「数值个数」判断：token 序列中数值样片段（数字开头）≥ 2 时，
          词条名取首个数值之前的片段、数值取最后一个——箭头及其误读杂字
          天然落在两数值之间被丢弃，不需要识别箭头本身
- Result: 强化页结算行（含带圈标记并存、箭头读出、箭头误读杂字三形态）、
          「新」角标剥离、单值行回归全部通过；列表页双数值行走读取失败
          有回归断言。两文件 51 passed，全量 324 passed
- Now:    「新」角标按行首剥离——词条名十族均不含「新」字，安全；带圈
          标记剥离（_ROLL_MARKERS）在「新」剥离之前，结算行「①暴击率
          3.5% 6.6%」先去 ① 再合并
- Convention: 数值样片段的正则锚定数字开头（[0-9][0-9.,%]*），杂字
          （箭头误读）不匹配即被排除；若 OCR 把两个数值并进单框无空格
          （如「5.8%11.1%」）则不触发合并、走解析失败——真实转储未见此
          形态，e2e 若暴露再处理

## 2026-08-29 · Task 1.1
- Tried:  四目录合并一步到位（mv pic2/3/4/*.png pic/ 后 rmdir），处理脚本
          只改来源常量与断言、fixture_stem 逻辑不动（「图N」映射规则与
          批次无关，合并后依然成立）
- Result: reference/pic/ 27 张（fig1~fig7、L1~L8、E1~E3、L9~L13、E4~E7），
          pic2/pic3/pic4 已删除；重跑处理脚本自检 27 张通过，原 18 张
          夹具逐字节不变（幂等），新增 9 张夹具待入库；README 三处口径
          更新（投放方式、扩测路径、验收记录标题与 E6 行）
- Now:    现行文档的 picN 引用仅剩 tasks/design（任务与决策记录本身）与
          discussion（声明不保持现行口径的历史底稿），均保留原样
- Convention: reference/pic/ 为唯一截图来源目录，后续补拍批次直接放入、
          不再开新目录；扩测时只调整 prepare_fixtures.py 的张数断言
