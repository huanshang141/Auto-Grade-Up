# CONTEXT.md — 领域术语表

本文件只收领域术语与定义，不写实现细节。设计文档、代码与讨论中的用词以本表为准。

## 游戏概念

- **圣遗物（Artifact）**：强化对象的统称——原神的圣遗物、星穹铁道的遗器、绝区零的驱动盘；本工具用同一套术语与机构处理，未指明具体游戏时，文档与界面中的「圣遗物」均指当前游戏的强化对象。
- **部位（Slot）**：圣遗物的固定类别，类别与数量由游戏档案声明（原神为五个：生之花、死之羽、时之沙、空之杯、理之冠）。
- **主词条（Main Stat）**：每件圣遗物固定有且仅有一个的基础属性。
- **副词条（Substat）**：圣遗物上的附加属性，随强化获得或成长；条数上限由游戏档案声明（原神为 4 条）。
- **待激活行**：未解锁副词条的灰色预览形态（初始词条不足上限时，显示下一批待解锁词条），两个界面均显示并带「（待激活）」标记；预览数值就是该词条解锁后的初始值（词条与数值由游戏预先生成）。词条种类与预览数值参与可达性推导、进入数据模型（带待激活状态；2026-09-06 修订，此前口径为整行丢弃）。
- **星级（Rarity）**：圣遗物的稀有度；范围由游戏档案声明（原神为 1~5 星）。
- **等级（Level）**：圣遗物的强化等级，0 到等级上限（原神上限为 20）。
- **锁定（Lock）**：游戏内的保护标记，锁定的圣遗物不会被当作狗粮消耗。
- **词条变动点**：强化过程中发生一次词条变动（新词条出现，或既有词条成长）的等级节点；间隔由游戏档案声明，原神为 +4/+8/+12/+16/+20。
- **阶段放入（Staged Fill）**：原神强化界面自带的批量放入狗粮功能；开启「阶段放入功能」后，放入量止步于下一个词条变动点（4/8/12/16/20 级节点）。

## 工具概念

- **游戏档案（game profile）**：声明一个游戏领域参数的配置文件——属性代号清单、部位代号清单、等级上限、词条变动间隔、星级范围、副词条上限、回合机制；每个游戏随其资源包维护一份，是字段、值域与剩余强化次数公式的唯一来源。
- **资源包（resource）**：MaaFramework 按游戏划分的素材与配置集合（导航流水线、模板图、OCR 模型、游戏档案、字段对照文档）；用户在运行器中选择资源包即选择游戏。
- **筛选条件（candidates）**：读取属性前的粗筛（星级、部位、等级上限、锁定状态），决定哪些圣遗物进入完整判定。
- **剩余强化次数**：从当前等级到等级上限还会发生的词条变动次数，等于 ⌈(等级上限 − 等级) ÷ 词条变动间隔⌉，向上取整（等级可能停在非节点上，如原神 +17 到 +20 仍有一次变动）；参数取自游戏档案，原神为 ⌈(20 − 等级) ÷ 4⌉。
- **强化判定（rule）**：同一棵条件树，既用于决定是否开始强化一件圣遗物，也用于每个强化回合后决定是否继续；输入为圣遗物当前属性与剩余强化次数。词条数值条件的判定口径是乐观可达值（2026-09-06 定案，见 ADR-0006）——规则要求的是「强化到底能否达到」，达成无望即提前止损（强化剪枝）。判定不通过即跳过或止损，因此决策机构不需要记住任何历史；同一件圣遗物被重复扫到时，同样按当前属性重新判定。
- **观测机构（observe）**：从游戏截图读取圣遗物属性、产出结构化数据的机构；按界面分列表页右栏与强化页两个读取器，目的相同（获取词条与属性）、输出同一数据模型，仅识别区域与文字格式不同。
- **附属读数（extras）**：观测机构读出、只写入运行报告、不参与强化判定的字段——等级内经验进度、本次强化摩拉、素材档位、面包屑指纹。
- **识别结果转储（OCR dump）**：从框架识别节点录制的「区域 → 文字框列表」数据文件；观测解析核心单元测试的输入夹具。
- **决策机构（rule_lambda）**：加载规则文件、对条件树求值、输出单一的「是否强化」布尔值与求值依据的机构；对每次输入独立求值，不保存历史状态，不感知所处界面。
- **执行机构（control）**：驱动批量遍历与强化操作的机构：选中卡片、滚动、进出界面、放入狗粮、点击强化、指纹核对；持有当前界面知识，把决策机构输出的布尔值按界面映射为操作。
- **导航流水线（pipeline）**：用声明式配置描述的界面操作流程（模板匹配找按钮、OCR 定位、等待动画），承担界面导航与带重试的等待。
- **字段对照文档（Field Mapping）**：游戏界面文字与内部字段代号之间的对照，独立于规则文件维护；观测机构用它把 OCR 结果适配为代号，使规则字段与游戏语言无关（套装名按原文比较，是第一版的例外）。收录词条名与部位名，随游戏资源包分发、按语言一份，第一版仅维护中文（多语言预留）。
- **属性代号（stat code）**：词条属性的内部英文名（如 crit_rate 表示暴击率）；固定值与百分比是两个不同代号，由字段对照文档与格式定义统一管理。
- **强化回合（Round）**：一次「阶段放入 + 强化」及其后的重新读取与判定；阶段放入止步于 4/8/12/16/20 级节点，一个回合恰好覆盖一个词条变动点。
- **强化次数（Roll Count）**：单个副词条被强化的累计次数；游戏内以词条旁的带圈数字显示（原神 5.3 版本新增的显示功能），解锁新词条不计入（解锁词条以「新」角标显示）。列表页不显示该信息（记为未知），强化页无标记即 0 次；未知与 0 在数据模型中是两种状态。
- **乐观可达值（Reachable Value）**：词条数值条件使用的判定口径——在最有利的成长分配假设下，该词条到等级上限为止能达到的数值上界；由决策机构按当前值（待激活词条用预览值）、剩余强化次数与档案中的单次强化成长上限推导，不硬编码游戏数值。
- **强化剪枝（Pruning）**：乐观可达性判定的自然结果——把剩余强化次数全部投入某个词条仍达不到规则要求时，判定不通过、提前放弃该圣遗物，节省无效强化（2026-09-06 需求方澄清：词条数值条件的本意即「最终可达」，剪枝与判定同源，见 ADR-0006）。
- **成长结算形态**：强化完成后界面上刚成长词条的显示形态（旧值 → 新值 ↑）；强化已完成，新值是当前值。标记保留到下一次强化完成或退出强化界面。
- **止损（Stop-loss）**：强化过程中因强化判定不通过而停止；止损的圣遗物保留在背包，绝不作为狗粮。
- **安全停止**：运行遇到无法自行恢复的异常（读取重试超限、界面与预期不符等）时停止全部操作并写出报告；宁可停止，绝不盲目点击消耗性按钮。
- **狗粮（Fodder）**：强化时被消耗以提供经验的其他圣遗物或材料。
- **指纹（Fingerprint）**：用于核对圣遗物身份的「部位 + 圣遗物名 + 等级」组合。
- **试运行（Dry Run）**：只执行扫描与判定、输出「将强化哪些圣遗物及理由」、不进入强化界面、不消耗任何资源的运行模式。
- **单件任务**：对用户指定的单件圣遗物跳过筛选条件、直接进入逐回合判定的运行方式。
