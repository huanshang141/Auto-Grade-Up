# CONTEXT.md — 领域术语表

本文件只收领域术语与定义，不写实现细节。spec、代码、对话中的用词以本表为准。

## 游戏域对象

- **圣遗物（Artifact）**：原神中可穿戴的装备件，本工具的操作对象。
- **部位（Slot）**：圣遗物的五个固定类别：生之花、死之羽、时之沙、空之杯、理之冠。
- **主词条（Main Stat）**：每件圣遗物固定有且仅有一个的基础属性。
- **副词条（Substat）**：圣遗物上的附加属性，最多 4 条，随强化获得或成长。
- **星级（Rarity）**：圣遗物的稀有度，1~5 星。
- **等级（Level）**：圣遗物的强化等级，0~20。
- **锁定（Lock）**：游戏内保护标记，锁定的圣遗物不可被作为狗粮消耗。

## 工具概念

- **候选过滤（Candidates Filter）**：在读取属性前的粗筛（星级、部位、等级上限、锁定），决定哪些圣遗物值得进入详情判定。
- **候选件（Candidate）**：通过候选过滤的圣遗物。
- **初始判定（Initial Check）**：对候选件读取完整属性后，决定是否开始强化的规则判定。
- **目标件（Target）**：通过初始判定、进入强化流程的圣遗物。
- **强化回合（Round）**：一次「阶段放入 + 强化」及其后的回读与判定，是逐回合判定的基本单位；阶段放入止步于 4/8/12/16 级节点，故一个回合恰好覆盖一个词条 roll 点。
- **词条 Roll 点（Roll Point）**：圣遗物 +4/+8/+12/+16/+20 的等级节点，发生词条变动（新词条解锁或既有词条成长）；逐回合判定以此为粒度。
- **逐回合判定（Round-wise Check）**：原称「逐级判定」。在每个强化回合结束后执行的继续/止损规则判定，判定对象为回读后的完整属性。
- **止损（Stop-loss）**：强化过程中因逐回合判定为「不再继续」而停止对该件强化；止损件保留在背包，绝不作为狗粮。
- **狗粮（Fodder）**：强化时被消耗以提供经验的材料（其他圣遗物或经验道具）。
- **阶段放入（Staged Fill）**：游戏强化界面自带的批量放入狗粮功能；开启「阶段放入功能」后，放入量止步于下一个 4/8/12/16 级节点。
- **指纹（Fingerprint）**：套装名 + 部位 + 等级 三元组，用于确认「当前界面上的圣遗物就是决策所针对的那一件」。
- **试运行（Dry Run）**：只执行扫描与判定、输出「将强化哪些件及理由」、不进入强化界面不消耗任何资源的运行模式。
